package onnx

import org.emergentorder.compiletime._
import org.emergentorder.io.kjaer.compiletime._

object Main {
    def main(args: Array[String]): Unit = {
        println("Class used to run some tests")
    }
}